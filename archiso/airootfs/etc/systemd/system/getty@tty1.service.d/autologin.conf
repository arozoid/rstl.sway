# rstl.sway live ISO - boot straight into the rstl-inst TUI on tty1.
# Auto-login root so there is no login/display-manager prompt; the root
# shell profile then execs rstl-inst (see /root/.bash_profile).
[Service]
ExecStart=
ExecStart=-/usr/bin/agetty --autologin root --noclear tty1 linux
